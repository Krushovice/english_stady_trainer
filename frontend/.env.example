# Base URL of the backend API. Defaults to this value in code if unset, so
# this file only matters if you need to point at something else (e.g. a
# non-default API_PORT).
VITE_API_BASE_URL=http://localhost:8000/api/v1
